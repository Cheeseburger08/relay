<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <instrumentation android:name="app.relay.companion.HistoryCheck" android:targetPackage="app.relay.companion" android:label="Verify authorized history sync" />
    <instrumentation android:name="app.relay.companion.PipeLatencyCheck" android:targetPackage="app.relay.companion" android:label="Synthetic pipe latency check" />
</manifest>
